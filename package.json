{
  "name": "music-club-website",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf _site && npx @11ty/eleventy",
    "start": "npm run build && npx @11ty/eleventy --serve",
    "format": "npx prettier --write ."
  },
  "keywords": [],
  "author": "",
  "license": "AGPL-3.0",
  "devDependencies": {
    "@11ty/eleventy": ">=0.12.1",
    "eslint": ">=7.28.0",
    "eslint-config-prettier": ">=8.3.0",
    "prettier": ">=1.3.1",
    "@prettier/plugin-pug": ">=1.15.2",
    "pug": ">=3.0.2"
  },
  "dependencies": {}
}
